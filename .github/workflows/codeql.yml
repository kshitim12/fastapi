name: "CodeQL"

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  schedule:
    - cron: '40 12 * * 0'

jobs:
  analyze:
    name: Analyze
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    timeout-minutes: ${{ (matrix.language == 'swift' && 120) || 360 }}
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'python' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v2
      with:
        languages: ${{ matrix.language }}

    - name: Autobuild
      uses: github/codeql-action/autobuild@v2

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v2
      with:
        category: "/language:${{matrix.language}}"
    
    - name: Check for Vulnerabilities
      id: check-for-vulnerabilities
      run: |
        if [ -e codeql-results/codeql-results.sarif ]; then
          echo "Checking for vulnerabilities..."
          if grep -q 'critical-vulnerability' codeql-results/codeql-results.sarif; then
            echo "Critical vulnerabilities found!"
            echo "Vulnerabilities found. Merging is disabled."
            echo "::set-output name=status::failure"
          fi
      working-directory: ${{ github.workspace }}
    
    - name: Prevent Merge
      if: steps.check-for-vulnerabilities.outputs.status == 'failure'
      run: |
       echo "Preventing merging due to vulnerabilities..."
       # Use the GitHub API to protect the branch
       # The branch_name variable is set to "master"
       branch_name="master"
       GITHUB_TOKEN=$GITHUB_TOKEN
       protection_url="https://api.github.com/repos/${{ github.repository }}/branches/${master}/protection"
       protection_data='{"required_status_checks": null, "enforce_admins": {"enabled": true}, "restrictions": null}'
       response=$(curl -X PUT -H "Authorization: token $GITHUB_TOKEN" -d "$protection_data" $protection_url)
       if [ "$response" = "" ]; then
        echo "Merge button disabled for branch $master"
       else
          echo "Failed to disable merge button for branch $master"
       fi
      env:
       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
       working-directory: ${{ github.workspace }}

